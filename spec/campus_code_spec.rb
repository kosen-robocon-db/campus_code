require "spec_helper"

describe CampusCode do
  it "has a version number" do
    expect(CampusCode::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
